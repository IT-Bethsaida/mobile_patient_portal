import 'package:flutter/material.dart';

class HospitalLogo extends StatelessWidget {
  const HospitalLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.local_hospital, size: 100, color: Colors.blue);
  }
}
